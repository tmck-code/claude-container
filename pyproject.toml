[project]
name = "claude-container"
version = "0.1.0"
description = "Docker image for Claude Code"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
  "awscli",
  "mypy",
  "ruff",
  "uv",
]
